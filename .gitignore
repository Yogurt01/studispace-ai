node_modules/
build/
dist/
coverage/
.vite/
.firebase/
firebase-debug.log*
firestore-debug.log*
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
*.tsbuildinfo
.DS_Store
Thumbs.db
.vscode/
.idea/
.env
.env.*
!.env.example
*.local
*.pem
firebase-service-account.json
service-account*.json
# Playwright
playwright-report/
test-results/
blob-report/

# This project builds with npm: CI, the Dockerfile and the deploy pipeline all
# run `npm ci` against package-lock.json. A stray `bun install` drops bun.lock
# here and deletes package-lock.json with it, which breaks every one of them.
bun.lock

# Test fixtures
material_for_test/
